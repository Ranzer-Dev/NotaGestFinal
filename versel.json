{
  "version": 2,
  "builds": [
    {
      "src": "microservico/server.js",
      "use": "@vercel/node"
    },
    {
      "src": "notagesexpress/server.js",
      "use": "@vercel/node"
    },
    {
      "src": "front/package.json",
      "use": "@vercel/next"
    }
  ],
  "rewrites": [
    {
      "source": "/api/auth/(.*)",
      "destination": "/microservico/server.js"
    },
    {
      "source": "/api/users/(.*)",
      "destination": "/notagesexpress/server.js"
    },
    {
      "source": "/(.*)",
      "destination": "/front/$1"
    }
  ],
  "installCommand": "npm install && npm install --prefix front && npm install --prefix microservico && npm install --prefix notagesexpress"
}